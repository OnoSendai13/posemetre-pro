{
  "name": "assistant-posemetre-pro",
  "version": "1.5.0",
  "description": "Assistant Posemetre Pro - Application professionnelle de posemetrie et flashmetrie pour photographes",
  "main": "index.html",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "npx http-server -p 8000 -c-1",
    "start": "npx http-server -p 8000 -c-1",
    "serve": "npx http-server -p 3000 -c-1",
    "build": "echo 'No build step needed - vanilla JS PWA'",
    "prebuild": "npm run validate",
    "validate": "echo 'Validating PWA...' && node scripts/validate-pwa.js || true",
    "ionic:serve": "npx http-server -p 8100",
    "ionic:build": "echo 'No build needed for vanilla JS'",
    "cap:init": "npx cap init",
    "cap:add:ios": "npx cap add ios",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open:ios": "npx cap open ios",
    "cap:open:android": "npx cap open android",
    "cap:update": "npx cap update",
    "deploy:gh-pages": "gh-pages -d .",
    "lighthouse": "npx lighthouse http://localhost:8000 --output html --output-path ./lighthouse-report.html"
  },
  "keywords": [
    "photography",
    "light-meter",
    "flash-meter",
    "exposure",
    "pwa",
    "capacitor",
    "pwabuilder",
    "progressive-web-app"
  ],
  "author": "Laurent Suchet IG:@ono_sendai",
  "license": "MIT",
  "homepage": "https://github.com/laurentsuchet/assistant-posemetre-pro",
  "repository": {
    "type": "git",
    "url": "https://github.com/laurentsuchet/assistant-posemetre-pro.git"
  },
  "bugs": {
    "url": "https://github.com/laurentsuchet/assistant-posemetre-pro/issues"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/app": "^8.1.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0",
    "@capacitor/haptics": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "http-server": "^14.1.1",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
